{
  "name": "ppo3-site",
  "private": true,
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "start": "npm run build && npx --yes serve site/dist -l 3000",
    "dev": "npm run build && npx --yes serve site/dist -l 3000"
  }
}
